server:
  port: 8080
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/own-movie?serverTimezone=UTC&ssl-mode=REQUIRED
    username: root
    password: test1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format-sql: true
        dialect: org.hibernate.dialect.MySQL8Dialect
